# Netlify Configuration File

[build]
  # No build process needed for this static site
  publish = "."
  functions = "netlify/functions"

# Environment variables (set these in Netlify dashboard)
# GOOGLE_MAPS_API_KEY = "your_api_key_here"

# Headers for security and caching
[[headers]]
  for = "/*"
  [headers.values]
    # Security headers
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    
    # Basic CSP - adjust as needed for your domain
    Content-Security-Policy = """
      default-src 'self'; 
      script-src 'self' 'unsafe-inline' https://maps.googleapis.com https://maps.gstatic.com; 
      style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; 
      font-src 'self' https://fonts.gstatic.com; 
      img-src 'self' data: https://maps.googleapis.com https://maps.gstatic.com https://*.googleusercontent.com;
      connect-src 'self' https://maps.googleapis.com;
      frame-src https://www.google.com;
    """

# Cache static assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache images
[[headers]]
  for = "*.png"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

[[headers]]
  for = "*.jpg"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

[[headers]]
  for = "*.svg"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

# Function-specific settings
[functions]
  directory = "netlify/functions"
  
# Optional: SPA fallback (uncomment if you convert to SPA later)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
#   conditions = {Role = ["admin"], Country = ["US"]}

# Redirect common paths
[[redirects]]
  from = "/home"
  to = "/"
  status = 301

[[redirects]]
  from = "/properties"
  to = "/listings.html"
  status = 301

# Form handling (if using Netlify Forms)
# Uncomment if you add netlify attribute to contact form
# [[redirects]]
#   from = "/contact"
#   to = "/contact.html"
#   status = 200

# Custom 404 page (optional)
# [[redirects]]
#   from = "/*"
#   to = "/404.html"
#   status = 404

# Edge functions (if needed in future)
# [[edge_functions]]
#   function = "geolocation"
#   path = "/api/location"

# Plugin configuration (optional)
# [[plugins]]
#   package = "netlify-plugin-lighthouse"
#   [plugins.inputs]
#     output_path = "reports"

# Development settings
[dev]
  framework = "static"
  command = "npx serve ."
  targetPort = 3000
  port = 8888
  publish = "."
  autoLaunch = true

# Context-specific settings
[context.production]
  environment = { NODE_ENV = "production" }

[context.deploy-preview]
  environment = { NODE_ENV = "preview" }

[context.branch-deploy]
  environment = { NODE_ENV = "development" }